// Tipos de exemplo: existem para demonstrar a convenção de `src/types/`.
// Substitua ou remova no projeto real.
export interface ExampleEntity {
    id: string
    nome: string
    ativo: boolean
}
